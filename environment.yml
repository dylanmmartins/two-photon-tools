# install: conda env create -f environment.yml
# update:  conda env update -f environment.yml

name: twop
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - jupyter
  - oasis-deconv
  - tqdm
  - pydantic
  - psygnal
  - pip:
    - scikit-learn
    - suite2p
    - pysimplegui
    - psygnal
    - pytomlpp
    - pyyaml
    - appdirs
    - h5py